# IntoDesign Studio - Уебсайт Портфолио

Едностраничен уебсайт, представящ портфолиото и услугите на студио за интериорен дизайн "IntoDesign".

## Основни функционалности

*   **Адаптивен дизайн:** Оптимизиран за различни размери на екрана (десктоп, таблет, мобилни устройства).
*   **Смяна на тема:** Възможност за превключване между светла и тъмна тема.
*   **Портфолио секция:** Галерия с реализирани проекти.
*   **Секция "Услуги":** Представяне на предлаганите пакети услуги.
*   **Секция "Екип":** Информация за дизайнерите в студиото.
*   **Форма за онлайн запитване:** Демонстрационна форма за контакт.
*   **Плавно превъртане:** При навигация през секциите на сайта.
*   **Анимации при скрол:** Елементите се появяват с фина анимация при достигането им.
*   **Чат страница:** Интерфейс за тестване на Cloudflare Workers AI с опция за дебат между два бота.
*   **Дебат между философи:** Платон защитава автоматизацията чрез AI, а Ницше изтъква ролята на човешкия труд. И двамата отговарят кратко и директно на опонента си.

## Технологии

*   HTML5
*   CSS3 (с CSS Custom Properties / Variables)
*   JavaScript (ES6+)
*   [Font Awesome](https://fontawesome.com/) (за икони)
*   [Google Fonts](https://fonts.google.com/) (Poppins & Inter)

## Структура на проекта

*   `main.html`: Основната HTML структура на страницата.
*   `style.css`: Всички CSS стилове за визията на сайта.
*   `script.js`: JavaScript код за интерактивност, смяна на тема, мобилно меню и анимации.
*   `chat.html` и `chat.js`: Примерен чат интерфейс за Cloudflare Workers AI.

## Персонализиране

Променливите за цветовата тема се намират в началото на `style.css`. Там са
дефинирани и стойностите за тъмния режим, например:

```css
:root {
    /* Dark theme variables */
    --bg-primary-dark: #121212;
    --bg-secondary-dark: #1e1e1e;
    /* ... */
}
```

Шрифтовете се зареждат чрез Google Fonts в `main.html`. Могат да бъдат сменени,
като се редактира `<link>` елемента и свойствата `font-family` в `style.css`.

## Как да разгледате сайта

1.  Клонирайте това хранилище или изтеглете файловете.
2.  Отворете файла `main.html` в предпочитания от вас уеб браузър.

### Чат страница

1. Отворете `chat.html`.
2. Изберете AI модел от падащия списък и въведете съобщение. Освен
   стандартния `llama-3.1-8b`, може да избирате `qwen1.5-7b-chat-awq`,
   `hf/thebloke/llama-2-13b-chat-awq` и още няколко по-мощни модела
   (Gemma 3, LLaMA 4 Scout, Mistral Small, LLaMA 3.3 70B FP8, LLaMA 3 - 8B).
   Обърнете внимание, че за модела Llama‑2 е необходим префикс `hf/`,
   иначе заявките ще връщат грешка.
3. Активирайте опцията **"Дебат"**, ако желаете разговор с два бота, и посочете втория модел.
4. В менюто **Настройки** има поле **"Общ промпт"**, който се добавя към инструкциите на двата бота.
5. С бутона **меню** (икона с три черти) можете да скриете или покажете панела с контролите. Избраното положение се запазва в браузъра.
6. По желание включете **"Автодебат"** и наблюдавайте как Платон брани автоматизацията, а Ницше се застъпва за хората. Опцията се спира от същата отметка или като напишете "стоп" в чата.
7. В този режим ботовете си четат репликите и се изчакват, за да спорят помежду си.
8. Отговорите се изписват с етикети **Платон** и **Ницше**, за да се проследява дебатът.
9. При нужда може да прикачите изображение или файл чрез бутона **"Качи файл"**.
10. Формата изпраща заявката към Worker бекенда и връща отговор от избрания модел(и).

#### Поддържани AI модели

| 🧠 Модел | 🏷️ Таг за извикване | 🧩 Кратко описание | 🇧🇬 |
|---------|--------------------|--------------------|-------|
| Gemma 3 - 12B Instruct | `@cf/google/gemma-3-12b-it` | Многоезичен модел с отлична поддръжка на български. Стабилен при дълги текстове, диалог и обобщения. Подходящ за есета, описания и обяснения | ⭐⭐⭐⭐⭐ |
| LLaMA 4 Scout - 17B Instruct | `@cf/meta/llama-4-scout-17b-16e-instruct` | Модерен Mixture-of-Experts модел с висока езикова точност. Естествен и плавен стил. Подходящ за диалог, идеи и аргументация | ⭐⭐⭐⭐☆ |
| Mistral Small 3.1 - 24B Instruct | `@cf/mistralai/mistral-small-3.1-24b-instruct` | Много силен при структурирано мислене и логически задачи. Отличен за разгръщане на сложни теми, креативен текст и разсъждения | ⭐⭐⭐⭐☆ |
| LLaMA 3.3 - 70B Instruct (FP8) | `@cf/meta/llama-3.3-70b-instruct-fp8-fast` | Най-мощният от всички. Дава изключително задълбочени и нюансирани текстове. Подходящ за философия, анализи и професионално съдържание. По-бавен | ⭐⭐⭐⭐☆ |
| LLaMA 3 - 8B Instruct | `@cf/meta/llama-3-8b-instruct` | Компактен, бърз и стабилен модел. Умерено задълбочен и подходящ за ежедневни задачи, създаване на съдържание и диалог | ⭐⭐⭐⭐ |
| Qwen 1.5 - 7B Chat AWQ | `@cf/qwen/qwen1.5-7b-chat-awq` | Универсален чат модел | ⭐⭐⭐⭐ |
| Llama-2 - 13B Chat AWQ | `@hf/thebloke/llama-2-13b-chat-awq` | По-голям модел за генерация на текст | ⭐⭐⭐⭐ |
| LLaMA 3.1 - 8B Instruct | `@cf/meta/llama-3.1-8b-instruct` | Стандартен модел за разговор | ⭐⭐⭐⭐ |

### Автодебат

Активирайте отметката **"Автодебат"**, за да оставите философите да спорят сами. Платон отстоява автоматизацията с AI, а Ницше брани човешкия труд.
Функцията `runDebateLoop()` извиква `handleSend()` на всеки три секунди и не спира,
докато изрично не напишете "стоп" или не изключите опцията от отметката.

API токенът за Workers AI е съхранен като secret в бекенда, така че не се налага да въвеждате ключ в браузъра.

**Забележка:** Формата за онлайн запитване в секция "Изпратете запитване" е само фронт-енд демонстрация и не изпраща реални данни. За пълна функционалност е необходима интеграция с back-end услуга за обработка на формуляри.

---

Разработено от [Вашето име/Име на екипа, ако е приложимо]
## AI Chat

[Отворете AI чата](chat.html) и следвайте стъпките:
1. Изберете модел от падащото меню.
2. Напишете съобщение и натиснете **Send**.
3. По желание включете **"Дебат"** с втори модел.

![Примерен изглед](assets/chat-screenshot.png)


### Worker AI бекенд

Скриптът `worker-backend.js` съдържа примерна Cloudflare Worker функция. По подразбиране тя използва модела, зададен в `MODEL`, но вече може да получава избрания модел чрез полето `model` в тялото на заявката. Необходими са променливи `ACCOUNT_ID`, `MODEL` и `AI_TOKEN`, конфигурирани като Worker Secrets.
Допълнително е нужен KV namespace `SETTINGS`, в който се пазят потребителските настройки.

Примерно добавяне на секрети с `wrangler`:

```bash
wrangler secret put ACCOUNT_ID
wrangler secret put MODEL
wrangler secret put AI_TOKEN
```

Ако някоя от тези стойности липсва, worker-ът ще върне грешка `Missing Worker secrets`.

Ако отваряте `chat.html` от различен домейн или локално като файл, Worker-ът трябва да връща CORS заглавки, за да позволи заявките. Примерните стойности са:

```text
Access-Control-Allow-Origin: *
Access-Control-Allow-Headers: Content-Type
Access-Control-Allow-Methods: POST
```

За да ги активирате, добавете ги в полето `headers` при създаване на `Response` в `worker-backend.js`:

```javascript
return new Response(result, {
  status: response.status,
  headers: {
    'Content-Type': 'application/json',
    'Access-Control-Allow-Origin': '*',
    'Access-Control-Allow-Headers': 'Content-Type',
    'Access-Control-Allow-Methods': 'POST'
  }
});
```

Worker-ът поддържа и маршрут `/settings` за четене и запис на тези настройки в
KV. `GET /settings` връща текущите стойности, а `POST /settings` ги обновява.

Допълнителни примери за заявки към различни AI модели ще откриете във файла `WORKERS_AI_EXAMPLES.md`.
